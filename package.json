{
  "name": "bijbelstudie-parasja",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-articles.mjs && node scripts/copy-static-assets.mjs && vite --host",
    "build:passages": "node scripts/build-passages-index.mjs",
    "build:epub": "node scripts/build-epubs.mjs",
    "build:downloads": "node scripts/build-download-manifest.mjs",
    "build:docs": "node scripts/build-docs.mjs",
    "build:articles": "node scripts/build-articles.mjs",
    "build:assets": "node scripts/copy-static-assets.mjs",
    "build:bible": "node scripts/build-bible-data.mjs",
    "build:domain": "node scripts/fix-built-domain.mjs",
    "build": "npm run build:passages && npm run build:epub && npm run build:docs && npm run build:downloads && npm run build:articles && npm run build:assets && npm run build:bible && vite build && npm run build:domain",
    "preview": "vite preview"
  },
  "dependencies": {
    "html-to-docx": "^1.8.0",
    "isomorphic-dompurify": "^2.36.0",
    "jszip": "^3.10.1",
    "lucide-react": "^0.474.0",
    "puppeteer": "^25.6.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^4.3.4",
    "js-yaml": "^5.2.3",
    "vite": "^6.1.0"
  }
}
